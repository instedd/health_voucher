%h2 Services &amp; Cost

.clinic_services
  %table.GralTable
    %colgroup
      %col(style="width: 55px;")
      %col(style="width: auto;")
      %col(style="width: 70px;")
      %col(style="width: 70px;")
      %col(style="width: 40px;")
    %tbody
      %tr
        %th Code
        %th Description
        %th Enabled?
        %th(colspan="2") Cost
      - @clinic.all_clinic_services.each do |cs|
        = render partial: 'clinic_service', locals: { cs: cs }

  .dialog#cost_dialog
    .box.grey.w20
      %span
        %a.fedit Set Service Cost
      .ux-content
        = form_tag nil, :method => "post" do
          = hidden_field_tag :service_id, nil
          .field.w20
            %label(for="cost") Cost
            %input.w20#cost(name="cost" type="number" value="" min="0" data-focus="true")
          .actions
            %button.grey(type="submit") Set

